CREATE DATABASE PagapocoDB;

USE PagapocoDB;

--  Users
CREATE TABLE User (
  Id UNIQUEIDENTIFIER PRIMARY KEY DEFAULT NEWID(),
  [Name] NVARCHAR(100) NOT NULL,
  Email NVARCHAR(100) NOT NULL,
  Phone NVARCHAR(50) NOT NULL,
  City NVARCHAR(100) NOT NULL
);

--  Publications (Base class)
CREATE TABLE Publication (
  Id UNIQUEIDENTIFIER PRIMARY KEY DEFAULT NEWID(),
  Title NVARCHAR(200) NOT NULL,
  [Description] NVARCHAR(MAX) NOT NULL,
  Price DECIMAL(18,2) NOT NULL,
  City NVARCHAR(100) NOT NULL,
  IsPremium BIT,
  Type NVARCHAR(50) NOT NULL, 
  UserId UNIQUEIDENTIFIER FOREIGN KEY REFERENCES Users(Id)
);

--  Vehicles (Subclass)
CREATE TABLE Vehicle (
  Id UNIQUEIDENTIFIER PRIMARY KEY FOREIGN KEY REFERENCES Publications(Id),
  Brand NVARCHAR(100) NOT NULL,
  Model NVARCHAR(100) NOT NULL,
  [Year] INT NOT NULL,
  Color NVARCHAR(50) NOT NULL,
  FuelType NVARCHAR(50) NOT NULL,
  Transmission NVARCHAR(50) NOT NULL,
  EngineDisplacement NVARCHAR(50) NOT NULL,
  KilometersDriven INT NOT NULL,
  [Version] NVARCHAR(100) NOT NULL,
  Doors INT NOT NULL,
  EnginePower INT NOT NULL
);

--  Bikes (Subclass)
CREATE TABLE Bike (
  Id UNIQUEIDENTIFIER PRIMARY KEY FOREIGN KEY REFERENCES Publications(Id),
  Brand NVARCHAR(100) NOT NULL,
  Model NVARCHAR(100) NOT NULL,
  [Year] INT NOT NULL,
  Color NVARCHAR(50) NOT NULL,
  FuelType NVARCHAR(50) NOT NULL,
  Transmission NVARCHAR(50) NOT NULL,
  EngineDisplacement NVARCHAR(50) NOT NULL,
  KilometersDriven INT NOT NULL,
  EnginePower INT NOT NULL,
  WheelSize NVARCHAR(50) NOT NULL
);

--  Parts (Subclass)
CREATE TABLE Part (
  Id UNIQUEIDENTIFIER PRIMARY KEY FOREIGN KEY REFERENCES Publications(Id),
  Brand NVARCHAR(100) NOT NULL,
  Model NVARCHAR(100) NOT NULL,
  Color NVARCHAR(50) NOT NULL,
  Condition NVARCHAR(50) NOT NULL,
  Compatibility NVARCHAR(200) NOT NULL
);

--  Images
CREATE TABLE Images (
  Id UNIQUEIDENTIFIER PRIMARY KEY DEFAULT NEWID(),
  [Url] NVARCHAR(500) NOT NULL,
  PublicationId UNIQUEIDENTIFIER FOREIGN KEY REFERENCES Publications(Id)
);

-- 💬 Queries (Preguntas)
CREATE TABLE Question (
  Id UNIQUEIDENTIFIER PRIMARY KEY DEFAULT NEWID(),
  [Name] NVARCHAR(100) NOT NULL,
  LastName NVARCHAR(100) NOT NULL,
  Phone NVARCHAR(50) NOT NULL,
  Content NVARCHAR(MAX),
  Date DATETIME DEFAULT GETDATE(),
  UserId UNIQUEIDENTIFIER FOREIGN KEY REFERENCES Users(Id),
  PublicationId UNIQUEIDENTIFIER FOREIGN KEY REFERENCES Publications(Id)
);

-- 🗨️ Responses (Respuestas)
CREATE TABLE Answer (
  Id UNIQUEIDENTIFIER PRIMARY KEY DEFAULT NEWID(),
  [Name] NVARCHAR(100) NOT NULL,
  LastName NVARCHAR(100) NOT NULL,
  Phone NVARCHAR(50) NOT NULL,
  Content NVARCHAR(MAX),
  Date DATETIME DEFAULT GETDATE(),
  UserId UNIQUEIDENTIFIER FOREIGN KEY REFERENCES Users(Id),
  QueryId UNIQUEIDENTIFIER FOREIGN KEY REFERENCES Queries(Id)
);
